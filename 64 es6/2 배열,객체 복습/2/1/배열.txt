배열,arrey
 var nameArr =[] 배열을 만들겠다는 뜻
 .push() = 배열의 가장 끝에 특정 값을 추가 한다.
 .unshift() =배열의 가장 앞에 특정값 추가한다.
 .pop()=배열의  가장 뒤의 항목을 제거한다.
  .shift() =배열의 가장 앞에 특정 항목 제거한다.
var leng=nameArr1.length=배열의 항목 길이를 표시해준다.
var index=nameArr1[0] =배열의 특정 위치를 표시해준다.
.splice(1,1)=인자(파라미터)가 2개일 경우엔 특정항목을 지운다. 해석) 1번째 항목에 1개를 지운다
.splice(1,1,'네')=인자(파라미터)가 3개일 경우 1번째에 1개를 지운뒤 '네'라는 문자열이 그자리에 들어간다